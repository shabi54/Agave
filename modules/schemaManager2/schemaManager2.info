<?php
//TODO: consider combining .module and .info... objects and extensions could just as easily be defined here

/**
 * Module meta data - run during install/uninstall
 */

function schemaManager2_info_meta() {
	return array(
		'name'=>'Schema Manager 2', //name of module in modules interfaces
		'machine-name'=>'schemaManager2', //name of directory in the modules folder
		'description'=>'Provides an object for managing dynamic data fields in the database. Displaying the data, and managing necessary form elements for changing the data. Uses an alternate data structure.',
		'group'=>'Core', //the category in which you module will appear on modules page, if 
		'version'=>'.1', //the current version of your module, increment this any time you make changes to db in your module
		'agave-version'=>'1', //what version of agave is required for this module to work?
		'dependencies'=>array('user'), //what other modules does this module rely on?
	);
}

function schemaManager2_info_actions() {
	$actions = array();
	//$actions[] = array('name','action','description','function','file','enabled?');
	
	return $actions;
}

function schemaManager2_info_cron() {
	$jobs = array();
//	$jobs[] = array('name','description','function','interval','enabled?');
	
	return $jobs;
}

function schemaManager2_info_menu() {
	$menus = array();
//	$menus[] = array('name','label','description');
	
	return $menus;
}

function schemaManager2_info_menu_items() {
	$items = array();
//	$items[] = array('menu','weight','label','desc','href','visible?','expanded?','access');
	
	return $items;
}

function schemaManager2_info_messages() {
	$msgs = array();
//	$msgs[] = array('type','handle','message');
	
	return $msgs;

}

function schemaManager2_info_panels() {
	$panels = array();
//	$panels[] = array('name','title','content','query','template','func','region','include','exclude','access','weight','enabled?');
	
	return $panels;
}

function schemaManager2_info_system_uri() {
	$uris = array();
//	$uris[] = array('dest','alias','access','function','file','panels','actions','fast_route');
	
	return $uris;
}

function schemaManager2_info_system_variables() {
	$vars = array();
//	$vars[] = array('key','PHP value');
	
	return $vars;
	
}

function schemaManager2_info_user_access() {
	$args = array();
//	$args[] = array('action','label','desc','default');
	
	return $args;
	
}

/**
	Update/modification hooks
*/
function schemaManager2_info_update() {
	//any special processing that should occur whenever update.php is run
}

function schemaManager2_info_install() {
	//anything special to run after installation
}

function schemaManager2_info_uninstall() {
	//anything special to run before uninstall
}